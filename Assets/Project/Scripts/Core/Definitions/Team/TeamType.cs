namespace Project.Core.Definitions.Team
{
    /// <summary>
    /// Categorises a team by its primary function within the company.
    /// MVP values only — additional types may be added when the GDD defines them.
    /// </summary>
    public enum TeamType
    {
        Design,
        Development,
        Support,
        Marketing
    }
}
